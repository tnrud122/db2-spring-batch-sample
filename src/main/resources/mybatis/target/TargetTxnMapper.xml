<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.batch.mapper.target.TargetTxnMapper">

  <insert id="insertTxn" parameterType="com.example.batch.domain.TargetTxnRecord">
    INSERT INTO TGT_TXN (ID, CUSTOMER_NM, AMOUNT, CREATED_AT, LOADED_AT)
    VALUES (#{id}, #{customerNm}, #{amount}, #{createdAt}, #{loadedAt})
  </insert>

</mapper>
